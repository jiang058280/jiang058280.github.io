# 博客首版改版交付与验收记录

日期：2026-09-16。状态：本地可预览，未提交、推送或发布，待作者反馈。

发布授权更新：作者已明确要求推送 GitHub 并发布正式网址。仓库 origin/main 已核实，Cloudflare Pages 与 GitHub Pages 均有自动部署检查。站点正式 URL 更新为 https://lgq-828.pages.dev；本次仅提交正在使用的改版资源，废弃实验与本地预览缓存不提交。最终生成目录 preview/2026-09-16T11-32-20-335Z，326 路由、163 原文章。

## 预览入口

- 首页：<http://127.0.0.1:4000/>
- 网页版简历：<http://127.0.0.1:4000/about/>
- 专题与面试复习：<http://127.0.0.1:4000/study/>
- 生成目录：`D:\boke\preview\2026-09-16T10-07-52-649Z`。
- 最终样式 Chrome 截图和报告：`D:\boke\preview\browser-2026-09-16T09-16-18-990Z`；随后仅修正上下篇标签并重新进行结构验证。

预览服务仅监听本机，不是公开网站。停止服务后可在项目根目录运行：

```powershell
node tools/serve-preview.cjs latest
```

新增代码后的安全预览生成：

```powershell
node tools/preview-build.cjs
```

此脚本每次生成新目录，不调用 `hexo generate` / `hexo clean`，不删除已有输出，不保存 Hexo 数据库。现有 `public/` 不变，因此直接打开旧 public 不能看到新版。不得未经确认改用会清理旧输出的普通构建或推送。

## 已实现

- 复习与关于我背景：各自独立明暗场景，阅读区半透明稳定底色，静态背景；只加载当前页当前主题资源，不增加持续动画或实时卡片模糊。用户批准本次四张生成目录外例外，项目副本均在 source/img，未删除旧素材或安装依赖。

- 最新反馈：浅色默认，保留显式深色选择；浅色模块整块遮罩取消，避免横向分割；主题选择框取消方形轮廓，保留圆角键盘焦点提示。

- 浅色背景重做：新晨光山水 JPEG 替代几何山峰，轻量局部文字承载底色和半透明卡片；深色与恢复后的柔和动态不变。浏览器报告 `preview/browser-2026-09-16T09-48-38-717Z`，浅色首页截图 `home-light.png`，确认使用新素材、动画开关与 PJAX 正常，无捕获脚本错误。结构验证 163 篇原文通过。首次沙箱浏览器超时后，使用获准隔离 Chrome 检查成功；没有安装工具。

- 最新反馈撤回：不再注入重制动态样式、SVG 云和月亮遮罩，不生成右下角周期设置；恢复柔和渐变薄雾与水光，保留刷新／滚动优化。下面动态重制条目仅为历史记录，已失效。实验源文件未删除，也未清除已有本机设置。

- 动态重制：天空与山谷矢量云层漂移，月亮与水面由单一遮光层控制亮暗同步；默认月光周期 6 秒，云周期 12 秒，首页右下角可调整并在本机保存。原图云朵未真正分离，动态层是叠加近似，不宣称所有原始像素云已移动。原素材不变，没有目录外写入或下载。

- 刷新／滚动优化：禁用内容入场动画造成的固定背景尺寸跳变，移除大卡片背景模糊和第二雾层，导航模糊降低至 6px，滚动时暂停动态、停止后恢复。保留原生滚动；未量化真实设备滚动帧率，不承诺恒定 60fps。

- 动态二轮修正：原动画虽然运行但过弱，且雾层有硬边；现增加流动幅度、缩短周期并加渐隐遮罩，首页固定背景贯穿全部模块，取消模块重复山水。浏览器复查目录 `preview/browser-2026-09-16T09-23-38-111Z` 无脚本异常，动画变化、暂停与减少动态通过；随后取消模块背景拼贴并重新构建。

- 月夜山水首页：个人定位、两个项目、四个专题、精选现有文章。
- 细边、弱高光与柔影的玻璃导航、卡片和按钮；紧凑导航不再显示左侧个人标识。
- 首页双层流动云雾与水面微光，按钮显示当前“动态：开／关”；系统减少动态时显示“系统静止”。
- 页面主滚动条白色，隐藏目录内部滚动条但保留滚动；深色阅读采用静态淡山水、墨青底、细金线及紧凑延伸阅读卡片。
- 浅色书斋阅读，深色阅读选项，桌面目录与手机可展开目录。
- 默认／浅色／深色主题，跨页保存显式偏好，减少动态和静止选项。
- 本地标题、分类、标签搜索，无结果和索引不可用提示；不宣称全文搜索。
- 完整网页版简历，按新版 PDF 整理个人职责和团队成果，保留电话及邮箱，无 PDF 上传、嵌入或下载。
- 隐藏文章时间、归档时间分组、相关文章卡片日期，保留底层日期与原始链接。
- 原图不可用时显示说明，未下载文章中的外链图片。
- 概念图指定底部标语、版权与装饰文案未引入。
- Agent 主项目未添加占位，原始资料与文章未删除。

## 自动验证结果

最新内页检查：`preview/browser-2026-09-16T10-09-14-830Z`，复习／关于我四种背景命中对应图片、背景铺满视口、桌面与手机无横向溢出、PJAX 与搜索正常，无捕获脚本错误。已修正旧主题将背景压为顶部内容块的问题；首次切换测试未等待旧主题过渡结束，补充等待后检查成功。

| 验证 | 结果与边界 |
|---|---|
| JavaScript 语法 | 新构建层、主题、交互、预览与检查脚本通过 |
| 独立生成 | 326 个输出路由，163 篇原文章 |
| 原文章访问 | 对 163 篇文章生成文件逐一检查，通过 |
| 元信息日期 | 文章、归档与相关文章的相关日期元素移除 |
| 简历 | 电话／邮箱链接正确，无 PDF 嵌入或下载，项目锚点存在 |
| 复习页 | 全部文章区含 163 个文章链接 |
| Chrome 页面 | 首页、简历、复习、文章页；桌面 1440px 和手机 390px 尺寸 |
| 版式 | 测试页面无页面级横向溢出，首页全宽，桌面与手机目录可见 |
| 交互 | 搜索 RAG 有结果；手机菜单打开与 Escape 关闭正常 |
| PJAX 与主题 | 首页／个人简介连续切换，默认主题正确，无重复导航 |
| 减少动态 | 模拟 prefers-reduced-motion 后云雾动画关闭 |
| 首页动态 | 浏览器测量云雾 transform 随时间变化，关闭后暂停，按钮明确显示当前状态 |
| 导航与滚动条 | 桌面导航宽度小于 1000px、无个人标识；目录 scrollbar-width 为 none，页面 scrollbar-color 为白色透明 |
| 深色阅读 | 自定义上下篇与延伸阅读卡片存在，桌面深色页尾已截图检查 |
| 脚本异常 | 最终 Chrome 检查未捕获 Runtime.exceptionThrown |
| Git 检查 | diff --check 通过；未提交或推送 |

## 未完成的验收与已知限制

- 尚未量化 LCP、CLS、INP，也未验证真实移动网络和低性能设备，不能宣称性能指标达标。
- 未测试 Safari、Firefox 和触屏实机；Chrome 手机尺寸模拟不等于 iPhone 实机测试。
- 未逐篇人工检查 163 篇文章的全部代码、公式、表格和图片；原文 Markdown 格式问题不在本次未经确认批量重写。
- 自动浏览器测试阻断外部资源，示例 RAG 文章的外链图片被阻断并显示降级提示；没有下载图片来解决该问题。
- 原有页面模板、不同姓名的自我介绍等参考资料仍保留。是否标注或清理需要作者另行确认。
- CSS 玻璃是可维护的模糊、透明度与高光近似，并非真实光学折射。
- 主标题与精选内容仍可由作者调整；当前交付不表示作者已经批准最终文案。

## 文件与环境保护记录

- 旧 `source/css/custom.css`、`source/js/custom.js`、`source/about/index.md` 保留；新版通过构建层和独立资源接管展示。
- 正常编辑了主题资源注入配置和 `.gitignore`，没有丢弃用户已有改动。
- 原文章与原 PDF 未修改，旧 `public/` 与 `db.json` 未清理。
- 没有安装、升级或下载任何软件、依赖、字体及工具。
- 本次月夜素材生成使用作者明确同意的单次目录外写入例外。项目压缩副本是 `source/img/moon-night.jpg`，约 128 KB；内置 imagegen 的完整提示词见 `scripts/guofeng/背景素材记录.md`。
- Chrome 使用新建于项目 preview 下的配置、缓存、临时文件及截图目录；仅对子进程设置 TEMP/TMP 路径，没有修改系统或用户环境变量。
- 测试结束关闭测试 Chrome，停止三个过时预览进程；仅保留最新本地只读预览服务。没有删除浏览器测试目录或旧预览文件。

## 建议的下一检查点

作者先查看本地首页、阅读页和简历，反馈玻璃透明度、字号、页面密度与项目文案。确认后进行针对性调整及更完整的排版和性能检查；发布仍需单独授权。
