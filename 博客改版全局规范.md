# 刘国庆个人博客改版：全局工作流程与规范

> 版本：2.0 · 2026-09-16
> 状态：首版已实现并通过本地结构及 Chrome 桌面／移动尺寸检查，待作者确认视觉与内容；未发布，性能与其他浏览器验收未完成。
> 本文是本次改版的需求基线与验收依据；历史《项目总结.md》用于了解旧实现，不代表本次新设计。

## 1. 目标与优先级

博客定位：有个人辨识度的 AI 应用开发工程师博客。

1. 第一目标：让面试官快速了解刘国庆的能力、个人贡献和项目经历。
2. 第二目标：方便作者按专题复习、搜索和阅读现有知识内容。
3. 视觉目标：动态中国风与现代、柔和的液态玻璃结合，不牺牲可读性与性能。
4. 时间原则：面试前优先交付完整可用的核心版本，增强功能后置。

不将博客定位局限为面试题库，也不以装饰替代能力展示。

## 2. 已确认需求与范围边界

| 项目 | 约定 |
|---|---|
| 首页 | 月夜山水，深墨蓝基调，动态克制 |
| 文章阅读页 | 明亮温润的宋韵书斋，正文清晰、阅读舒适 |
| 模块材质 | 更通透、更柔和的液态玻璃；减弱效果图里的厚亮边、强高光与凸起感 |
| 明暗切换 | 提供手动切换，同时保留默认分页面主题 |
| 简历 | 以新版简历为事实来源，制作符合站点风格的完整网页版简历，不嵌入或上传 PDF |
| 联系方式 | 网页版简历保留作者手机号和邮箱，作者已确认公开 |
| 文章时间 | 不展示发布、上传与更新时间；不编造或随机分配历史日期 |
| Agent 项目 | 作者已有相关项目，但当前暂不整理、不展示占位内容；以后可补到项目首位 |
| 当前动作 | 用户已授权按计划实施；可以修改项目代码，仍不得擅自下载、删除、修改电脑环境或发布网站 |

### 2.1 截图指定删除项

用户指定可删除的是概念图底部内容：

- 左侧“山高水长，技术为伴。”标语。
- 右侧“© 2026 刘国庆 · 技术手记”及其后的装饰性文案。

正式实现不添加上述内容及等价的填充性页脚文案；当前只记录，不执行删除。此要求不扩展为删除所有导航、功能入口或文章内容。

### 2.2 第一版不做

- Agent 项目文案、演示与简历增补。
- 在线大模型问答、模拟面试、账户系统、后台管理。
- 复杂 WebGL 场景、视频背景、背景音乐、全屏强制开场动画。
- 未经确认的文章删除、批量重写、URL 改动。
- 未经确认的公开部署、提交推送与敏感信息公开。

## 3. 现有项目基线

本地检查结果，不代表已核实线上版本：

- 技术：Hexo + Butterfly，原生 CSS / JavaScript 自定义。
- 内容：`source/_posts/` 中现有 163 篇 Markdown 文章。
- 分类包括提示词工程、Dify、Coze、Python 基础、数据库、并发、网络编程、每日分享及面试环节。
- RAG 内容分布在多个分类中，需要新增专题聚合，而非立即改动全部原分类。
- 现有桌面导航是由脚本生成的左侧导航坞；新方案为顶部悬浮导航，需要处理旧逻辑冲突。
- 已启用 PJAX；页面切换不一定触发整页加载。
- 现有主题使用 `html[data-theme]`，新主题必须统一管理，不建立互相竞争的状态。
- 当前配置已关闭页脚版权相关信息，不重新引入概念图装饰页脚。

关键文件：

| 路径 | 作用 |
|---|---|
| `_config.yml` | 站点、文章链接与生成配置 |
| `_config.butterfly.yml` | 主题、菜单、资源注入、PJAX 配置 |
| `source/css/custom.css` | 旧视觉样式 |
| `source/js/custom.js` | 导航及页面交互 |
| `scripts/content_index.js` | 构建时生成全站文章索引 |
| `source/about/index.md` | 当前个人简介 |
| `source/_posts/` | 原始文章，不随视觉改版随意删除 |

## 4. 信息架构与页面规范

### 4.1 首页：能力优先

顺序：悬浮导航 → 个人主视觉 → 精选项目 → 核心技术专题 → 精选文章。

- 导航：删除左侧印章、姓名和技术手记，使用紧凑居中的首页、项目、技术专题、面试复习、关于我、搜索、主题与动态切换。
- 项目和专题在第一版可使用首页锚点，不为凑菜单建立空页面。
- 个人区：真实姓名、AI 应用开发方向、简短定位、项目与文章入口。
- 主标题“在山水间，构建智能。”仅作为候选文案，不视为最终个人口号。
- 项目：先展示新版简历中的智慧教研平台和智能工单系统；项目卡片链接至实际详情，不使用无效按钮。
- 专题：RAG、Agent 与工作流、提示词工程、Python 与工程基础等；Agent 专题可聚合已有知识文章，不等同于展示 Agent 项目。
- 精选文章：手工挑选能体现原理、取舍、实践或复盘的现有文章；未选定时不捏造文章。
- 首页不铺满全部面试题，不出现“敬请期待”的 Agent 项目空卡。

### 4.2 项目详情

每个项目统一结构：业务问题 → 技术架构 → 个人职责 → 关键取舍 → 团队成果 → 验证或复盘。

- 明确“负责”和“参与”，保留新版简历的贡献边界。
- 团队指标必须标为团队成果，不改写成个人独立完成。
- 架构图只表达已确认实现，不加入未经说明的技术。
- 没有公开源码或演示就不显示相应按钮，不把私有项目当开源项目展示。
- 不公开公司内部资料、客户数据、密钥或有保密风险的截图。

### 4.3 文章阅读页

- 默认温润书斋：浅米白、墨色正文、低存在感纸张纹理。
- 标题可用中文衬线字体；正文优先清晰的中文无衬线；代码使用等宽字体。
- 正文底色接近不透明，不使用透出动态山水的强玻璃。
- 桌面正文建议最大宽度 760–840px，字号 17–18px，行高 1.8–1.95。
- 目录与工具区可弱玻璃；不遮挡内容，标题锚点留出导航高度。
- 代码块可横向滚动并支持复制；检查复制失败提示和数学公式、表格、图片显示。
- 手机正文建议 16–17px，侧边距至少 16px；宽表格独立滚动。
- 深色阅读模式使用稳定墨色底，不套用首页的高装饰主视觉。

### 4.4 专题、复习与搜索

- 保留现有分类、标签、分页与文章访问路径。
- 专题使用映射聚合旧文章，可一篇归入多个专题；无需第一版批量改 front matter。
- 第一版搜索为本地标题、分类、标签搜索，明确不宣称全文搜索。
- 若增加全文检索，另行设计索引体积、加载方式及代码与 HTML 清洗。
- 列表显示标题、分类和标签，不显示文章日期；长标题不挤坏布局。
- 搜索支持键盘操作、关闭、无结果提示和索引失败降级。
- 复习进度、收藏、随机抽题作为后续增强，不阻塞核心版本。

### 4.5 个人简介与简历

新版事实来源：`D:/新建文件夹 (2)/个人简历/刘国庆-AI应用开发工程师-新版简历.pdf`。

- 工作时间以新版的 2025.06–2026.08 为准。
- 突出数据清洗、资料解析、父子分块、知识库建设、MySQL / Milvus 职责边界、检索联调、来源引用与证据不足拒答。
- 新版未写入的 Agent、LangGraph、多智能体、LoRA 项目，不武断认定为不存在，也不自行补进公开项目经验。
- 个人简介与简历合并为符合站点风格的完整网页：个人信息、专业技能、工作经历、项目经历、个人优势与技术说明；清晰区分个人职责和团队成果，不机械堆叠 PDF 版式。
- 旧简历模板、不同姓名的自我介绍等先盘点并标记参考资料，未经确认不删除。
- PDF 仅用于本地核对事实，原始文件保持不变；不复制到站点、不嵌入 PDF 阅读器、不提供 PDF 下载按钮，不链接作者电脑的本地盘符。
- 作者已确认保留联系方式：手机号 `18435722256`，邮箱 `18435722256@168.com`。在网页版简历展示，可提供 `tel:` 与 `mailto:` 链接；其他个人信息不因这项授权自动扩大公开范围。

### 4.6 文章时间展示与真实性

作者确认现有文章时间集中或相同，不适合作为知识积累的展示依据。采用隐藏时间方案，而不是伪造更早的发布日期。

- 首页精选文章、文章列表、文章页元信息、搜索结果、分类、标签、专题与复习入口均不展示发布、上传或更新时间。
- 不制作按年份、月份排列的视觉时间线；现有归档 URL 保留可访问，页面改为无日期文章索引，去掉时间分组和单篇日期。
- “发表”“更新于”等文字与对应图标一并移除，避免残留空位。
- 保留 Markdown 的 `date` / `updated` 等底层数据；当前 permalink 含年月日，直接删除或修改日期可能改变链接，不能为隐藏显示而损坏原 URL。
- 文章索引可保留内部日期字段用于既有排序，但渲染不展示；不将集中导入时间描述为真实创作历史。
- HTML 元数据、RSS 与 sitemap 如继续包含时间，只保留原始记录，不伪造历史；URL 中原有日期也不因本需求改写。因此本方案是取消页面时间展示，不承诺所有机器可读数据中完全无日期。
- 简历中的真实工作时间、项目中有依据的时间以及文章正文内有意义的日期不删除。
- 若以后需要更正日期，只使用能够核实的真实时间，并单独制定链接兼容与重定向方案。

## 5. 设计系统

### 5.1 色彩与字体

以下是首轮实现候选参数，以浏览器实际观感和对比度为准：

| 语义 | 月夜 | 书斋 |
|---|---|---|
| 页面底色 | `#091521` | `#F5F1E8` |
| 主文字 | `#F1EBDD` | `#252D29` |
| 次文字 | `#BBC9CE` | `#626A60` |
| 青绿强调 | `#A4C8B9` | `#496B5A` |
| 暖金强调 | `#C9AE75` | `#927644` |
| 印章 | 克制的朱红 | 克制的朱红 |

- 用 CSS 语义变量统一管理色彩、玻璃、圆角、阴影、间距与动画。
- 衬线字体用于短标题，不将书法字体用于大段正文和代码。
- 字体与图标优先自托管或系统回退，避免为视觉增加大体积外部字体依赖。
- 国风来自山水、留白、排版、色彩与个人印章，不大量堆砌古诗、祥云和边框。

### 5.2 柔和液态玻璃

用户指定方向：比上一张图更通透、柔和、自然，不厚重、不塑料化。

- 导航：半透明墨蓝玻璃、背景模糊、极细边缘高光，悬浮但不过度凸起。
- 项目卡：适中圆角 18–24px；柔和内渐变与阴影；透出模糊山水，但文字区域有足够底色。
- 专题卡：更轻的材质，高光弱于项目卡。
- 按钮：适度胶囊形；不为标签、图标和每个箭头都加独立强高光。
- 禁止粗白发光边、多层硬描边、夸张镜面光斑、彩虹折射、强外发光。
- 不对整页、正文和每个嵌套元素重复应用模糊。
- 真实光学折射不作为第一版承诺；优先使用可维护的 CSS 半透明、模糊、渐变和细高光近似。
- “更通透”不意味着无限降低不透明度：复杂背景下自动提高文字承载底色。
- 不支持 `backdrop-filter` 时降级为可读的半不透明面板，不丢失交互。

### 5.3 主题行为

提供“默认 / 浅色 / 深色”三种选择：

- 默认：根据页面类型决定；首页月夜，文章阅读书斋，其余内容页默认书斋。
- 浅色或深色：用户显式选择后覆盖所有页面并持久化。
- 返回默认：清除显式偏好，恢复分页面规则。
- 状态采用同一主题管理器，兼容 Butterfly 的 `data-theme`。
- 首次加载尽早设置主题，避免闪白；PJAX 切页重新识别页面类型。
- 禁用存储时仍可当次切换，不报错。
- 背景素材和组件颜色均随主题适配，不只反转文字色。

## 6. 动态、性能与无障碍

### 6.1 动态规范

- 首页最多采用 1–2 种低频背景动态：云雾缓移、水面微光；不让所有景物同时运动。
- 卡片交互建议 160–240ms，悬停位移不超过 2–4px。
- 正文阅读区域不持续运动，不做打字机或逐字展开。
- 优先动画 `transform` / `opacity`，避免持续改变模糊半径、阴影与布局。
- 触屏不依赖 hover；页面不可见时暂停持续动画。
- 遵循 `prefers-reduced-motion`，提供动态关闭选项；关闭后功能和视觉层次仍完整。

### 6.2 性能规范

- 首页背景使用压缩 WebP / AVIF 与合适分辨率，首轮目标主背景不超过约 500KB，依据画质测试调整。
- 首屏关键图不懒加载，非首屏图片懒加载；固定图片尺寸避免跳动。
- 不为玻璃效果引入大型框架或重型特效库。
- 手机减少模糊强度和动态层数，不使用易抖动的固定背景策略。
- 浏览器测试记录 LCP、CLS、INP；目标 LCP ≤ 2.5s、CLS ≤ 0.1、INP ≤ 200ms，实验室结果和实际用户数据分开说明，不宣称未经测试的达标。

### 6.3 无障碍规范

- 普通文字对比度目标至少 4.5:1，大文字至少 3:1；在实际背景与透明度合成后检查。
- 点击目标建议至少 44×44px；焦点清晰、键盘可达。
- 图标按钮有可访问名称，表单有标签；不只用颜色表达当前状态。
- 搜索与移动菜单打开后管理焦点，Escape 可关闭，关闭后归还焦点。
- 装饰山水不制造无意义朗读；信息图片有准确替代文本。

## 7. 工程实现规范

### 7.1 工作目录与电脑环境保护（必须遵守）

以下为作者明确要求，优先于本文件其他工作流程中的默认操作：

- 本项目唯一允许的写入目录为 `D:\boke` 及其子目录。文档、代码、背景素材、截图、预览输出、日志、测试结果、临时文件与构建产物都必须留在此目录内，不向其他盘符、用户目录、系统临时目录或其他项目目录写入项目内容。
- 用户提供的目录外简历与参考图片可以只读查看，不修改原文件。需要保存项目副本时，只能保存到 `D:\boke` 内，并遵守下载、复制及敏感信息相关约定。
- 使用工具前检查其输出路径与副作用。若工具必然向目录外生成文件、缓存或项目内容，不能直接使用，也不能先生成到外部再搬回；必须先说明原因、目标路径和影响，并获得作者对该例外的明确同意。
- 不擅自下载任何文件、素材、字体、依赖或工具，不擅自安装、升级或重装软件。网络下载、包管理器安装以及运行时自动下载都必须先说明内容、用途与保存位置，并获得作者同意。
- 优先使用已经存在的工具与依赖；缺少依赖时报告缺项并请求同意，不通过其他工具或方式绕过确认。
- 不擅自修改电脑环境：包括全局依赖、系统配置、环境变量、PATH、注册表、权限、安全设置、系统服务、启动项、浏览器配置及项目外的软件配置。确有必要时，先说明具体改动、风险与恢复方法，获得同意后才能执行。
- 不擅自删除作者文件，包括项目内文件、旧资料、图片、临时文件和生成产物；删除、清理、覆盖无法恢复的数据或重置操作均须先明确列出目标与影响，并获得同意。
- `npm run clean` / `hexo clean`、递归清理、强制 Git 重置等可能删除内容的命令不得作为默认步骤执行。普通构建也须先核查是否会自动清理文件、写入目录外或自动下载依赖。
- 任务范围内正常代码和文档编辑可以进行，但必须先检查已有改动并保留无关内容；整文件替换、丢弃已有改动、覆盖原始素材等有数据丢失风险的操作须先征得同意。
- 不因目录可写、工具可用或存在历史审批而推定作者已同意本次下载、删除或环境变更。授权范围不清楚时停止该项操作并询问，不先执行后通知。
- 检查失败、工具限制或路径限制必须如实报告；不能为完成效果图或验证而突破上述边界。

历史说明：本条约束建立前，效果图生成与简历检查曾产生位于项目目录外的预览文件。本条生效后不再向那些目录写入；已有文件也不自行删除或移动，需要作者另行批准。

### 7.2 项目实现与变更管理

- 沿用 Hexo / Butterfly，不默认重写为新的前端框架。
- 不直接修改 `node_modules` 的主题源码；优先站点级配置、模板扩展、脚本和样式。
- 首页核心文案、项目和链接优先构建时生成，避免仅由 JS 插入；搜索等增强可客户端执行。
- 整理或替换旧侧栏与科幻个人档案样式，不在旧样式末尾无限叠加覆盖。
- 页面类型使用稳定标识识别，不只凭路径中是否出现某个词。
- PJAX 初始化幂等；事件不重复绑定；切页释放动画、监听器和浮层。
- 文章索引继续构建时生成；展示采用安全文本处理，不直接注入未经清洗内容。
- 静态资源更新同步调整缓存版本；保留原文章 permalink。
- 处理全部本地修改前查看工作区状态，保留无关用户改动。
- 文档更新与代码更新同步；新增事实须可追溯，生成效果图不得作为项目事实来源。

## 8. 完整实施流程与检查点

### 阶段 A：盘点与内容确认

产物：页面清单、旧样式冲突清单、项目事实表、精选文章候选、文章时间展示位置清单。

- 核实构建环境、依赖、现有路由与部署配置，不根据历史总结直接改版本。
- 记录当前主要页面截图与构建结果，作为改版基线。
- 对照新简历区分个人职责与团队成果。
- 只标记旧参考资料，不立即删除。

检查点：没有遗漏现有访问路径，确认网页版简历信息范围，PDF 不进入站点资源目录。

### 阶段 B：视觉定稿与资源准备

产物：柔和玻璃首页、书斋文章页、移动端关键布局预览，以及必要背景资源。

- 基于已确认月夜效果图减弱玻璃厚边和高光。
- 单独准备无文字背景；不要把整张 UI 效果图当真实网站背景。
- 定稿桌面与手机的导航、项目卡、正文、主题选择和搜索样式。
- 允许针对玻璃观感一次集中反馈，不重新打开已确认的整体风格讨论。

检查点：文章页与首页属于同一设计系统，玻璃不抢文字，截图指定页脚未出现。

### 阶段 C：基础设计系统与导航

产物：语义变量、页面主题管理器、悬浮导航、移动菜单、基础组件。

- 先解决旧左侧导航、隐藏顶栏和主题状态冲突。
- 建立默认 / 浅色 / 深色和减少动态规则。
- 完成静态布局后再增加轻动态。

检查点：跨页面与 PJAX 切换正确，无重复导航、闪白或遮挡。

### 阶段 D：首页与项目展示

产物：能力优先首页、两个项目详情或有实际内容的项目区域。

- 放入已核实个人定位与项目内容。
- 项目、专题、文章按钮全部有实际目标。
- Agent 项目不占位，数据结构保留后续调整排序的空间。

检查点：访客无需进入题库即可了解方向、项目和个人贡献。

### 阶段 E：阅读与复习

产物：书斋文章模板、目录、代码复制、专题聚合、本地搜索与复习入口。

- 对代表性的长文、代码、数学、表格、图片文章逐一检查。
- 保留全部文章、分类、标签与分页。
- 搜索显示范围与实际索引能力一致，失败时有替代导航。
- 移除各页面的文章时间显示与归档时间分组，不改底层日期及原文章 URL。

检查点：阅读清晰，搜索有效，手机操作顺畅。

### 阶段 F：个人简介与简历同步

产物：与书斋风格一致的完整网页版个人简介及简历页面，保留手机号和邮箱。

- 按新版简历组织网页内容，不上传、嵌入或提供 PDF 下载。
- 不修改原始 PDF，不自动替作者补写 Agent 经历。
- 旧资料的标注或清理另按确认范围执行。

检查点：网页与新版简历事实一致，团队成果归属正确，手机号和邮箱保留，PDF 未进入发布目录。

### 阶段 G：验证与交付

产物：可构建代码、本地预览、验收记录、已知限制与发布说明。

- 运行 `npm run build`，检查静态输出和关键链接。
- 桌面与手机浏览器验证；无法测试的浏览器明确记录，不假装已通过。
- 对比改版前后文章数量与访问路径；测试 PJAX、搜索、切换、菜单、代码复制。
- 检查控制台错误、网络失败、性能、对比度与减少动态模式。
- 先交付本地可预览版本；推送可能触发自动部署，须先确认发布授权。

检查点：下方验收项完成，未完成项如实列出。

## 9. 验收清单

- [ ] 首页默认月夜，文章默认书斋，手动主题跨页持久化。
- [ ] 玻璃细边、弱高光、柔影，不出现效果图式厚亮包边。
- [ ] 截图指定的底部标语、版权与装饰文案不出现。
- [ ] 两个项目的职责与团队成果和新简历一致。
- [ ] 不出现 Agent 空项目，不将作者未写进简历的项目误判为不存在。
- [ ] 完整网页版简历符合站点风格，保留手机号和邮箱，原 PDF 未修改、未上传、未嵌入。
- [ ] 首页、文章页、列表、分类、标签、搜索和归档不显示文章时间或时间分组，未伪造日期。
- [ ] 全部原文章可访问，原 permalink 保留。
- [ ] 目录、代码、数学、表格和图片正确呈现。
- [ ] 搜索、专题、分页、移动菜单、主题切换有实际功能。
- [ ] PJAX 连续切页无重复监听、重复元素或残留浮层。
- [ ] 手机无页面级横向溢出，必要代码和表格局部滚动。
- [ ] 减少动态、无模糊支持、存储不可用及索引失败时均可使用。
- [ ] 构建成功，关键页面无未处理脚本错误；浏览器测试范围有记录。
- [ ] 未经授权未推送、公开部署或删除旧资料。
- [ ] 本次操作的项目文件、临时产物及工具输出均位于 `D:\boke` 内；目录外写入例外须有事先明确授权。
- [ ] 未经同意未下载、安装或升级任何文件、依赖或工具，未修改电脑环境。
- [ ] 未经同意未删除、清理、重置或覆盖有数据丢失风险的文件和已有改动。

## 10. 待确认事项与默认决策

不需要继续逐项询问颜色、圆角与动画参数，可按本规范先推进设计预览。

| 事项 | 处理 |
|---|---|
| 简历形式与联系方式 | 已确认：完整网页，保留手机号和邮箱，不上传或嵌入 PDF |
| 文章时间 | 已确定：隐藏页面时间展示，保留原始底层日期与 URL，不伪造历史 |
| 首页最终主标题、个人印章 | 使用候选进行预览，定稿前确认，不凭空设定个人信条 |
| 精选文章 | 从现有内容挑选候选，优先原理与实践，不编造 |
| 旧模板、自我介绍 | 默认保留，提出标注方案，不擅自删除 |
| 公开源码、项目演示 | 仅在确有资源且获准公开时添加 |
| Agent 增补 | 当前跳过，作者后续提供时另行整理 |

## 11. 变更与后续扩展

### 当前实施记录

- 折叠菜单反馈：标题、导航及底部控件左对齐；点击菜单外部自动收起并同步 aria-expanded，不阻止页面原有点击行为；菜单内部控件操作不自动关闭，Escape 保留。本轮未发布。

- 最新导航决定：移除顶部常驻悬浮页眉，改成右下角折叠菜单，保留全部导航／搜索／主题／动态入口；Escape 关闭与键盘焦点继续支持。本轮仅本地预览，未发布。

- 文章布局反馈：1400px 及以上屏幕以屏幕中心定位 860px 正文面板，目录移入右侧留白、最大 260px；较窄桌面沿用双栏，手机展开目录不变。本次仅本地预览，未自动推送新布局。

- 复习与关于我独立背景：经用户明确确认本次四张素材生成例外，imagegen 生成浅色竹林书斋、深色月下竹影、浅色江南庭院、深色临水亭阁。四个 JPEG 站点副本保存 `source/img/*-v1.jpg`，内页使用稳定页面标识跨 PJAX 切换背景，首页保持不变。阅读面板不加实时模糊与持续动画，全部原文与简历内容保留。提示词及原始位置见 `scripts/guofeng/内页背景素材记录.md`。

- 最新主题规则覆盖此前月夜默认约定：默认所有页面浅色，用户显式深色偏好仍保留。浅色内容区大片伪元素遮罩取消，背景跨模块连续；卡片底色保留。主题选择框无方形 outline，键盘 focus-visible 使用柔和金色圆角提示。

- 浅色重做：用户本轮同意单次素材生成目录外例外，内置 imagegen 新生成精细晨光山水，站点副本 `source/img/morning-shanshui-v2.jpg`（272449 字节），由 `source/css/guofeng-light.css` 接管浅色首页；不再使用几何山峰，保留深色及柔和薄雾、水光。完整提示词与授权见 `scripts/guofeng/浅色背景素材记录.md`。未安装、下载依赖或删除素材。

- 最新决定优先：用户否决重制的硬边云层和月亮遮光，已停止注入 guofeng-motion.css、停止生成动态 SVG 和周期设置，恢复重制前柔和薄雾／水光；刷新、滚动优化保留。实验文件原地保留但不参与页面展示，不删除文件；浅色新素材尚未获准生成，本轮不推进。

- 重制动态：`scripts/guofeng/scene.html` 提供天空／山谷独立矢量云层及同相位月亮、水面遮光层；背景图片本身保持原样，不是逐朵分离原图云。首页右下角可设置月光周期 2–60 秒、云周期 4–60 秒，默认 6／12 秒，本机存储，无账户或网络写入。浅色保留青绿山水，不显示月光遮罩；滚动与减少动态规则保留。

- 刷新与滚动优化：禁用旧主题内容入场 transform 动画，避免固定背景临时变成整页大小；大面积项目／专题卡去掉实时 backdrop blur，导航仅 6px 模糊；动画层合成提示，移除第二雾层，滚动时短暂停动态，停止 140ms 后恢复。使用原生滚动，不引入滚动劫持库。尚未证明实机帧率，不能保证所有设备恒定 60fps。

- 动态加速与浅色背景：云雾周期 3.5 秒（手机 4 秒），水光 3 秒，关闭动态与系统减少动态继续有效。浅色首页独立使用项目内编写的 `source/img/morning-landscape.svg`：暖日、青绿层峦与湖面，不用夜景洗白；无新下载、安装或目录外素材生成。录屏未成功解码，不宣称已观看其动态。

- 后续动态反馈：云雾改为 8 秒往返，增加可感知位移与透明度变化，使用纵向渐隐遮罩消除矩形截断；首页使用贯穿页面的固定背景，取消内容区重复背景拼贴。文章阅读仍保持静态。输出更新见验收记录。

- 新增 `scripts/guofeng.js`：构建时首页、专题复习页面和主题级时间清理；不修改原文章日期。
- 新增 `source/css/guofeng.css` 与两个 `guofeng` 脚本：新版样式、主题、本地搜索及导航；旧 custom 文件保留，仅更换注入入口。
- 新增 `scripts/guofeng/resume.html`：网页版完整简历，不修改原始 PDF，不将 PDF 放入发布目录。
- 新增 `tools/preview-build.cjs`：独立、唯一预览目录，使用内存数据库，不调用会清理旧文件的 generate / clean，不写回 db.json。
- 新增本地校验与预览服务脚本；`preview/` 已加入忽略规则，没有删除任何预览文件。
- 首次结构预览有 315 个输出路由、163 篇文章；原文章路由、首页项目、专题、联系方式、时间隐藏与主题状态通过结构检查。
- 用户已明确批准仅本次背景素材生成使用目录外写入例外。内置 imagegen 已生成无文字月夜背景，压缩副本保存为 `source/img/moon-night.jpg`（约 128 KB），没有安装或下载其他工具。完整提示词与授权边界记录于 `scripts/guofeng/背景素材记录.md`。
- 最新本地预览：`http://127.0.0.1:4000`，输出位于 `preview/2026-09-16T09-18-39-022Z`，317 个路由、163 篇文章；只读预览服务自动选择最新完成的输出。
- Chrome 无界面检查覆盖首页、复习页、网页版简历、文章页以及 390px 手机尺寸；确认无横向溢出、目录可见、搜索有结果、PJAX 切页主题正确、导航不重复、减少动态有效，未捕获脚本异常。
- 截图及浏览器报告位于 `preview/browser-2026-09-16T09-16-18-990Z`。检查使用项目内独立浏览器配置，不修改日常浏览器配置，外部页面资源请求被阻断；旧测试服务已停止，仅保留最新预览服务。
- 本轮反馈：缩窄导航；首页双层云雾与水面微光增强，开关显示当前状态；目录内部滚动条隐藏但滚动保留，页面主滚动条白色；深色阅读采用静态淡山水、墨青底、细金线与紧凑延伸阅读卡片。浏览器已验证动画实际变化、关闭暂停、手机布局和减少动态；随后仅修正上下篇标签并重新结构构建。
- 性能指标、其他浏览器、触屏实机及所有复杂文章排版未全面验收，不能标记整体验收通过。详细范围见《改版验收记录.md》。

- 需求变更记录日期、来源、影响页面与验收标准。
- 顺序：内容真实性与可用性 > 阅读与性能 > 风格一致性 > 装饰复杂度。
- 后续可增加 Agent 主项目、复习进度、收藏与随机抽题。
- 博客 RAG 问答助手需另立方案：引用、拒答、评测、费用限制、密钥后端管理及隐私，不能把 API 密钥放静态前端。

### 变更记录

| 日期 | 版本 | 内容 |
|---|---|---|
| 2026-09-16 | 1.0 | 建立月夜首页、书斋阅读、柔和玻璃、简历同步、Agent 后置及截图页脚删除的全局规范 |
| 2026-09-16 | 1.1 | 简历改为完整网页并保留电话邮箱，取消 PDF 上传与嵌入；文章隐藏时间展示，保留底层日期与链接，不伪造历史 |
| 2026-09-16 | 1.2 | 限定项目写入仅在 D:\boke 内；下载、安装、删除、清理及电脑环境变更必须事先获准，并约束工具自动副作用 |
| 2026-09-16 | 1.3 | 紧凑无个人标识导航、明确动态状态、单一可见白色滚动条及墨青山水深色阅读；保留目录独立滚动能力 |
| 2026-09-16 | 1.4 | 加快云雾与水光，新增独立晨光青绿矢量背景，所有新素材在项目内创作 |
| 2026-09-16 | 1.5 | 修复刷新背景尺寸跳变，降低玻璃和动画渲染成本，滚动暂歇动态、停止恢复 |
| 2026-09-16 | 1.6 | 独立云层漂移，月亮与水面明暗同相位，增加本机周期设置；原图未拆层或改写 |
| 2026-09-16 | 1.7 | 按用户要求撤回 1.6 动态重制，恢复柔和薄雾水光，保留性能修复；实验文件保留未删除 |
| 2026-09-16 | 1.8 | 获准单次生成精细晨光浅色山水，替换几何背景展示，保留深色与原柔和动态 |
| 2026-09-16 | 1.9 | 默认浅色，取消浅色模块大片遮罩与分割线，主题焦点改圆角提示 |
| 2026-09-16 | 2.0 | 复习／关于我四套独立明暗场景，静态背景与稳定阅读面板，保留首页与原内容 |
